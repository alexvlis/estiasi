spring:
  application:
    name: booking-service
server:
  port: 8080

---
spring:
  profiles: dev
  application:
    name: booking-service
server:
  port: 9090

---
spring:
  profiles: prod
  application:
    name: booking-service
server:
  port: 4431